language: node_js
node_js:
  - lts/*
os:
  - osx
  - linux
dist: focal
jobs:
  include:
    - os: windows
      env: YARN_GPG=no
script:
  - if [ $TRAVIS_BRANCH == "master" ] && [ $TRAVIS_PULL_REQUEST == "false" ]; then
      yarn release;
    else
      yarn build;
    fi
